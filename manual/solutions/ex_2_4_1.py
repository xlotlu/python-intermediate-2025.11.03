# Declare a variable called my_age and assign it your current age as an integer.
# Print the value.
my_age = 10
print(my_age)

# Declare a variable called distance and assign it a length in meters as a
# float. Print the value.
distance = 7.8
print(distance)

# You decide you actually want to store the distance as an integer. Convert
# distance created above to int and assign the result to the same variable.
# Print the new value.
distance = int(distance)
print(distance)
